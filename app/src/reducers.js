export const initialState = {
  loginStatus: false,
  username: "000000000",
  password: ""
};

export function codecharacterReducer(state, action) {
  switch(action.type) {
    default: {
      return state;
    }
  }
}
